#ifndef CLIENT_H
#define CLIENT_H

#include "protocol.h"
#include <map>

/*template <size_t n, size_t k>
class Client
{
    std::string name;
    KeyPair<n,m> keypair;

public:
    Client(const std::string &name);
    bool sendMessageTo(const Client &other);
    bool receiveMessageFrom(const Client &other);
};

// template <size_t n, size_t k>
// class Server
// {
//     std::map<
// }

// template <size_t n, size_t m>
// Client<n,m>::Client(const std::string &name)
//     : name(name)
// {}

// template <size_t n, size_t m>
// bool Client<n,m>::sendMessageTo(const Client &other)
#endif // CLIENT_H
